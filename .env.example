# Copy to .env and fill in real values. .env is gitignored — never commit it.
# VAPID keys for Web Push (phase 2d). Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:0mandrock1@gmail.com
